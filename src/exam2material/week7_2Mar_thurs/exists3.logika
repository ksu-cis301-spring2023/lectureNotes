∃ x (P(x)) ⊢ ∃ y P(y)
{
    1. ∃ x P(x)                 premise
    2. {
        3. alias P(alias)       assume
        4. ∃ y P(y)             ∃i 3 alias
        //want: ∃ y P(y)
    }
    5. ∃ y P(y)                 ∃e 1 2
    //want: ∃ y P(y)
}